# Python dependencies for the analysis pipeline (scripts 01-04, 06, 07).
# Tested on Python 3.12. Versions are unpinned; pin them (e.g. `pandas==x.y.z`)
# if you need bit-for-bit reproducibility.

numpy
pandas
pyarrow          # Parquet read/write backend used by pandas
pyreadstat       # Reads NHANES SAS XPORT (.XPT) files (scripts 01, 02)
lifelines        # Weighted Cox proportional-hazards models (scripts 03, 04, 06)
statsmodels      # Weighted least squares with cluster-robust SEs (script 07)

# The interventional survival-mediation confirmation (script 05) runs in R,
# not Python. See README ("R environment") for the required R packages.
